{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|Update",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$CLAUDE_TOOL_ARGS\" | grep -i \"any\\|Any\"; then echo \"🚨 WARNING: CLAUDE.md VIOLATION - Detected Any/any usage! Type widening is FORBIDDEN. Use precise types (Protocol, TypeGuard, Union, Literal, etc.) instead.\"; exit 1; fi'"
          }
        ]
      }
    ]
  }
}